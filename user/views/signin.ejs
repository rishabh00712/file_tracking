<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rishabh's Login Page</title>
    <link rel="stylesheet" href="/css/styles.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
</head>
<body>
    <% if (typeof error !== 'undefined') { %>
        <div id="notification-bar" class="notification">
            <%= error %>
        </div>
    <% } %>

    <div class="container">
        <h1>Research & Consultancy Cell of IIEST, Shibpur</h1>
        <div class="form-block-3">
            <form id="email-form" name="email-form" action="/signin" method="post">
                <div class="input-group">
                    <input class="text-field-3" type="email" name="email" id="email" placeholder=" " required autofocus>
                    <label for="email">Enter Your Email</label>
                </div>
                <div class="input-group">
                    <input class="text-field-4-copy" type="password" name="password" id="password" placeholder=" " required>
                    <label for="password">Enter Your Password</label>
                    <span class="toggle-btn" id="togglePassword">👁️</span>
                </div>
                <div class="div-block-4">
                    <div class="button_box">
                        <input type="submit" class="button" value="SIGN IN">
                        <button type="button" class="button" onclick="window.location.href='/signup';">SIGN UP</button>
                    </div>
                    <a href="/change_pass">FORGOT PASSWORD</a>
                </div>
            </form>
        </div>
    </div>

    <script>
        // Toggle password visibility
        const passwordInput = document.querySelector('.text-field-4-copy');
        const togglePassword = document.getElementById('togglePassword');

        togglePassword.addEventListener('click', () => {
            const currentType = passwordInput.getAttribute('type');
            const newType = currentType === 'password' ? 'text' : 'password';
            passwordInput.setAttribute('type', newType);

            // Toggle the eye icon
            togglePassword.textContent = currentType === 'password' ? '🙈' : '👁️';
        });
        //noticaiton bar 
        window.addEventListener('DOMContentLoaded', () => {
            const bar = document.getElementById('notification-bar');
            if (bar) {
            bar.classList.add('show'); // Slide down

            setTimeout(() => {
                bar.classList.remove('show'); // Slide back up
            }, 3000);
            }
        });
    </script>
</body>
</html>